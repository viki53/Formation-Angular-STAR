import { BusLateDirective } from './bus-late.directive';

describe('BusLateDirective', () => {
  it('should create an instance', () => {
    const directive = new BusLateDirective();
    expect(directive).toBeTruthy();
  });
});
